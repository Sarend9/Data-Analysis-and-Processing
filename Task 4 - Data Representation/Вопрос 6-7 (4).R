# Вопрос 6-7: В отчётности предприятия должности ассистента, специалиста, 
# старшего специалиста, консультанта обозначены соответственно кодами:
# 1, 2, 3, 4. Ниже представлены данные о должностях сотрудников одного из отделов:

position_at_work <-c(2, 2, 3, 2, 1, 1, 3, 2, 1, 1, 2, 1, 2, 4, 2, 2, 3, 2, 1, 1, 1, 1, 1, 1, 1, 1, 2, 2, 2, 1, 2, 1, 2, 3, 3, 2, 2, 3, 1, 1, 1, 3, 2, 3, 1, 1, 3, 1, 2, 1, 1, 1, 1)
  
  # Таблица частот:
  freq_table <- table(position_at_work)
  cat("Таблица частот:\n")
  print(freq_table)
  cat("\n")

# Определите:

  # Объем наблюдений
  obs_count <- length(position_at_work)
  cat("Объем наблюдений:", obs_count, "\n\n")
  
  # Относительная частота
  relative_freq <- prop.table(freq_table)
  cat("Относительная частота:\n")
  names(relative_freq) <- c("ассистент", "специалист", "старший специалист", "консультант")
  print(relative_freq)
  cat("\n")
  
  # Относительная накопленная частота
  cumulative_relative_freq <- cumsum(relative_freq)
  cat("Относительная накопленная частота:\n")
  print(cumulative_relative_freq)
  cat("\n")
  
  cat("Относительная накопленная частота (%):\n")
  print(cumulative_relative_freq * 100)
  
  # Постройте диаграмму накопленных частот
  barplot(cumulative_relative_freq * 100, 
          main = "Накопленные частоты встречаемости должностей в отделе", 
          xlab = "Должность",
          ylab = "Накопленная частота, %")
